from setuptools import setup,find_packages

setup(name='BalanceEvolution',version='0.1',
      requires=['scipy','numpy','datetime'],
      author='Optimized Options',
      packages=['BalanceEvolution'])


